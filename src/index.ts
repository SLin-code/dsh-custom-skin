/** Host half for the DSH Web skin client plugin. */

/** Cordis plugin name. */
export const name = 'dsh-custom-skin'

/** The feature is implemented entirely in the browser client. */
export function apply(): void {}
